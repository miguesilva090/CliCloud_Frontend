/**
 * Re-exporta os estilos padrão de formulário do projeto.
 * As abas de edição de utente usam o mesmo standard que o resto da aplicação.
 */
export {
  fieldGap,
  labelClass,
  inputClass,
  selectTriggerClass,
  buttonIconClass,
  textareaClass,
  formBlockGap,
} from '@/lib/form-styles'
