/**
 * Pesquisa por nome na barra superior; painel expansível não tem critérios extra
 * nesta listagem — mantemos a assinatura do DataTable para o modal de filtros, se existir.
 */
export function ListagemCentrosSaudeFilterControls(_props: {
  table: any
  columns: any[]
  onApplyFilters: () => void
  onClearFilters: () => void
}) {
  return null
}
