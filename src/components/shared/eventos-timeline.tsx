/**
 * Stub: timeline de eventos desativado (escopo antigo removido).
 * Reimplementar quando houver eventos no domínio ativo.
 */
interface EventosTimelineProps {
  eventos: unknown[]
  className?: string
}

export function EventosTimeline(_props: EventosTimelineProps) {
  return null
}
